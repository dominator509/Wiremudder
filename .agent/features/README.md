# Feature Authority

`FEATURES.tsv` is the machine-readable catalog. A row can change only with source reconciliation, specification update, graph review, test and proof update, and ADR when scope is removed or materially altered.
