# Requirement Traceability

`VALIDATION_MATRIX.tsv` maps every numbered behavior in accepted specifications to an owning node, expected test location, verification class, and live-fire proof. `scripts/spec-trace-check.sh` is a hard gate.
