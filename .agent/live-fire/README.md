# Live-Fire Proofs

Each declared proof script is created by its owning node under `tests/live-fire/`. `scripts/live-fire.sh` fails loudly when a required proof is absent or does not print its exact sentinel.
