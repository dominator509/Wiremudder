# Runtime State

`LEDGER.md` is append-only. Evidence directories, source evidence, command locks, leases, and release indexes are created by graph nodes. State never becomes product specification authority.
