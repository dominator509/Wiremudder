[package]
name = "wire-policy"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "WireMudder command policy core: per-world command database, risk tiers, deny/allow rules, argument validation, Human-Tempo anti-spam pacing (SPEC-009)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
