[package]
name = "wire-tactical"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "WireMudder Tactical HUD: bounded current-state snapshots, observer only, never sends commands (SPEC-012-R07, EP-020)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
