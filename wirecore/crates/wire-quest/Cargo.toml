[package]
name = "wire-quest"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "WireMudder Quest Compass: cited quest tracking with observed, inferred, completed, failed, and user-corrected state (SPEC-012-R06, EP-020)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
