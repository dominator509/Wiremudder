[package]
name = "wire-replay"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "WireMudder deterministic session replay, diagnostic bundles, and sanitized fixtures (SPEC-019, SPEC-024, SPEC-026, EP-028)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
