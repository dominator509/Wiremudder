[package]
name = "wire-routing"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "WireMudder network routing profiles: explicit user-owned routes, connect-time validation, no silent fallback, user-triggered egress verification, routing audit (SPEC-006)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
