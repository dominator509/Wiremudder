[package]
name = "wirecore-runtime"
version = "0.1.0"
edition = "2021"
license = "GPL-2.0-or-later"

[dependencies]
wire-contracts = { path = "../wire-contracts" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
