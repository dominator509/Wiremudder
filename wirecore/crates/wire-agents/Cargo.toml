[package]
name = "wire-agents"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "WireMudder specialized agent registry, Agent Skill Tree, role-scoped deny-by-default memory permissions, and budgeted Agent Council with disagreement records (SPEC-014, EP-018)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wire-soul = { path = "../wire-soul" }
