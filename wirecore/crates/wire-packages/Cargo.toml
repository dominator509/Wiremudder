[package]
name = "wire-packages"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "WireMudder package core: manifests, permission firewall, provenance, quarantine, and safe update semantics (SPEC-008, SPEC-020, SPEC-022)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
