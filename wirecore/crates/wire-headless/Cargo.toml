[package]
name = "wire-headless"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "WireMudder Multi-Session, Headless CLI, and Supervisor (SPEC-017, SPEC-024, EP-023)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
