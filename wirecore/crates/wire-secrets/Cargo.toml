[package]
name = "wire-secrets"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "WireMudder Secrets Vault core: secret classes, backends, leak redaction (SPEC-010-R06/R07)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
