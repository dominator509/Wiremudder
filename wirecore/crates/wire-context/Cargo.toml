[package]
name = "wire-context"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "WireMudder context distillation: deterministic typed events, context capsules with spam removal, redaction (SPEC-003, SPEC-013, EP-015)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
