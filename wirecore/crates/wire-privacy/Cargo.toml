[package]
name = "wire-privacy"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "WireMudder privacy core: egress policy, consent registry, redaction engine (SPEC-010, SPEC-022)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1.13.1"
