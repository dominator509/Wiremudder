[package]
name = "wire-actions"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "WireMudder deterministic Action Proposal gateway: risk tiers, confirmations, visible queue, cooldowns, complete audit, global emergency stop (SPEC-009, SPEC-004)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wire-policy = { path = "../wire-policy" }
