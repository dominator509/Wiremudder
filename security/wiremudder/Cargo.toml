[package]
name = "wiremudder-security"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "WireMudder security core: threat model, secrets scan, prompt-injection guard, supply-chain inventory, SBOM, license inventory, and release blocking (EP-033)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
