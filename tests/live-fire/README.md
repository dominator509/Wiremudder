# Live-Fire Scripts

Owning nodes create one script for each row in `.agent/live-fire/PROOFS.tsv`. A proof drives the real application or headless entry point against real controlled dependencies, asserts observable effects, tears down, and prints `LF-NNN proof-name: ok` only on success.
