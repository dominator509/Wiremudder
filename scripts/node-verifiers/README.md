# Node Verifiers

Each graph node creates `EP-XXX.sh` with `M1`, `M2`, `M3`, `M4`, `M5`, and `verify` modes. A mode runs real checks and prints exactly `EP-XXX Mx: ok` or `EP-XXX verify: ok` only on success. A verifier never edits production code or fabricates evidence.
